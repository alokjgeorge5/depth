[phases.setup]
nixPkgs = ["python310", "python310Packages.pip"]

[phases.install]
cmds = [
  "python -m venv /opt/venv",
  "cd backend && /opt/venv/bin/pip install -r requirements.txt"
]

[start]
cmd = "cd backend && /opt/venv/bin/gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 2"
